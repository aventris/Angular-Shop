import { TruncatecartPipe } from './truncatecart.pipe';

describe('TruncatecartPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncatecartPipe();
    expect(pipe).toBeTruthy();
  });
});
